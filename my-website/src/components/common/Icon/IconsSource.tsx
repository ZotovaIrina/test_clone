import * as React from 'react';
import { ReactComponent as Icons } from '../../../images/icons.svg';

const IconsSource: React.FunctionComponent<{}> = props => {
  return <Icons style={{ display: 'none' }} />;
};

export default IconsSource;
